export const selectIsModalOpen = (state) => state.modal.isOpen;
export const selectModalContactId = (state) => state.modal.contactId;
export const selectModalContactName = (state) => state.modal.contactName;
export const selectModalPosition = (state) => state.modal.position;
